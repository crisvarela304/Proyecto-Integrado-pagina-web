# =============================================================================
# Environment Variables for Production - Schoolar OS
# Copiar este archivo a .env.prod y configurar los valores
# =============================================================================

# Django
SECRET_KEY=cambiar-por-una-clave-secreta-muy-larga-y-segura
DEBUG=False
ALLOWED_HOSTS=tu-dominio.cl,www.tu-dominio.cl

# CSRF
CSRF_TRUSTED_ORIGINS=https://tu-dominio.cl,https://www.tu-dominio.cl

# Database (PostgreSQL)
POSTGRES_DB=schoolar_db
POSTGRES_USER=schoolar_user
POSTGRES_PASSWORD=cambiar-esta-password-muy-segura
DB_HOST=postgres
DB_PORT=5432

# Gunicorn
GUNICORN_WORKERS=4

# Colegio (Phone Home)
COLEGIO_NOMBRE=Nombre de tu Colegio
COLEGIO_URL=https://tu-dominio.cl

# CORS (orígenes permitidos para la App móvil)
CORS_ALLOWED_ORIGINS=exp://localhost:8081,http://localhost:8081

# Superusuario inicial (opcional)
CREATE_SUPERUSER=true
SUPERUSER_PASSWORD=admin123-cambiar-en-produccion
